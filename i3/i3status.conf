general {
	colors = true
	interval = 5
}

order += "load"
order += "ethernet eno1"
order += "time"

ethernet eno1{
	format_up = "%ip"
	format_down = "disconnected"
}

load{
	format = "%1min %5min %15min"
}

time{
	format = "%m-%d-%Y %a %I:%M:%S"
}
